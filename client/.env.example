# Optional: If backend API is hosted on a different port or domain, define it here:
# Example: VITE_API_URL=http://your-server-ip:5000
# Example: VITE_API_URL=https://api.yourdomain.com
# Leave empty or omitted if frontend and backend are served from the same domain or reverse-proxied.
VITE_API_URL=
